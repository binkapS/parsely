import 'package:binkap_parsely/src/element.dart';

class ParselyData {
  ParselyData({
    required this.element,
    required this.match,
  });
  final ParselyElement element;
  final Match match;
}
